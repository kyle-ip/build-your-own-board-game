{
  "name": "board-game-design-guide-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix site",
    "build": "npm run build --prefix site",
    "preview": "npm run preview --prefix site",
    "lint": "npm run lint --prefix site"
  }
}
